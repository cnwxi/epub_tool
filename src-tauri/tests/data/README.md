# Test data

`ttf-parser-demo.ttf.hex` is a lowercase hexadecimal encoding of
`tests/fonts/demo.ttf` from `ttf-parser` 0.25.1. It is used only to verify
TrueType cmap rewriting and is available under `MIT OR Apache-2.0`, matching
the upstream crate.
